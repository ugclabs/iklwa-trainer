name: Check trainer script
on: [push, pull_request]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Syntax check
        run: bash -n iklwa-trainer.sh
      - name: Run selftest
        run: bash iklwa-trainer-selftest.sh
